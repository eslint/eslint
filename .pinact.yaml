version: 3

ignore_actions:
    # Reusable workflows from eslint/workflows are maintained by the ESLint team
    # and referenced by branch, so they can't be pinned to a SHA.
    - name: eslint/workflows/.*
      ref: main
