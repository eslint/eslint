"use strict"

module.exports = {
    configs: {
        recommended: {
            plugins: ["@scope"],
            rules: { "@scope/rule": "error" }
        }
    },
    rules: {
        rule: {
            create(context) {
                return {
                    Program(node) {
                        const message = "OK"
                        context.report({ node, message })
                    }
                }
            }
        }
    }
}
