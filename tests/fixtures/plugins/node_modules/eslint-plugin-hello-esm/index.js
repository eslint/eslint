const plugin = {
    rules: {
        hello: {
            create(context) {
                return {
                    Program(node) {
                        context.report({ node, message: 'Hello ESM!' });
                    }
                };
            }
        }
    },
};

export default plugin;
