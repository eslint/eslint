module.exports = {
    rules: {
        rule1: {
            create(context) {
                return {
                    Program(node) {
                        context.report({ node, message: `Hello` });
                    }
                };
            }
        }
    },
};
