module.exports = {
    rules: {
        rule1: {
            create(context) {
                context.report({
                    node: context.sourceCode.ast,
                    message: "Rule report from plugin"
                });

                return {};
            }
        }
    }
}
