module.exports = {
    rules: {
        rule1: {
            meta: {
                schema: []
            },
            create(context) {
                return {
                    Program(node) {
                        context.report({ node, message: `Hello` });
                    }
                };
            }
        }
    },
};
